# Syntax Verifier Config File
d_model: 256
n_heads: 4
n_layers: 6
dropout: 0.1
max_seq_len: 256 
# no_error, unexpected_indent, unexpected_dedent,
# invalid_syntax, missing_colon, unmatched_bracket,
# unmatched_paren, unmatched_brace, invalid_token,
# unterminated_string, undefined_name, type_error
syntax_error_classes: 12
verify_every: 16